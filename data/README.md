# Data

Place downloaded sample clouds here. Prefer external storage for large files. `data/raw/` is gitignored, including any `seg_out/` the classical segmentation script writes next to a local preview.

The IntCDC preview path the segmentation script uses by default is `data/raw/darus-intcdc/preview.ply`. It is not in git.
